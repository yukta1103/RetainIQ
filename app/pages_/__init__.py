"""Dashboard pages.

Named `pages_` rather than `pages` on purpose: Streamlit auto-discovers a
top-level `pages/` directory and would build its own navigation from it,
conflicting with the st.navigation setup in streamlit_app.py.
"""
