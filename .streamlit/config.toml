[theme]
# Pinned to light. The chart palette was validated against the light surface
# (#fcfcfb); a dark theme would need its own validation run rather than an
# automatic flip, so it is deliberately not offered.
base = "light"
primaryColor = "#2a78d6"
backgroundColor = "#f9f9f7"
secondaryBackgroundColor = "#fcfcfb"
textColor = "#0b0b0b"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
